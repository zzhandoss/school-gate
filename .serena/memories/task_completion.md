# Task Completion Checklist

- Run `pnpm lint` (if it fails, try `pnpm lint:fix`).
- Run `pnpm typecheck`.
- Run `pnpm test`.
- Run `pnpm build`.
- Fix underlying issues if any checks fail (do not weaken tests).
